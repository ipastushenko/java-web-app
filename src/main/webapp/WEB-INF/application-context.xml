<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans-3.0.xsd">

    <bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="locations" value="file:${java_web_app.config.path}/jdbc.properties" />
    </bean>

    <!-- Data Source -->
    <bean id="dataSource" class="org.postgresql.ds.PGPoolingDataSource">
        <property name="dataSourceName" value="${jdbc.dataSourceName}" />
        <property name="serverName" value="${jdbc.serverName}" />
        <property name="databaseName" value="${jdbc.databaseName}" />
        <property name="user" value="${jdbc.user}" />
        <property name="password" value="${jdbc.password}" />
        <property name="maxConnections" value="${jdbc.maxConnections}" />
    </bean>

    <!-- Transaction Manager -->
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource" />
    </bean>

    <!-- Sql Session factory -->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource" />
        <property name="configLocation" value="classpath:mybatis-config.xml" />
        <property name="mapperLocations" value="classpath:mappers/*.xml" />
    </bean>

    <!-- MyBatis scanner -->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="com.ipastushenko.core.repository.mapper" />
        <property name="sqlSessionFactoryBeanName" value="sqlSessionFactory" />
    </bean>

    <!-- User Service -->
    <bean id="userDetailsRepository" class="com.ipastushenko.core.repository.UserDetailsRepository" />
    <bean id="userDetailsServiceImpl" class="com.ipastushenko.core.service.UserDetailsServiceImpl" />
</beans>