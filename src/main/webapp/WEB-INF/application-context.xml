<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd">
    <!-- Data Source -->
    <bean id="dataSource" class="org.postgresql.ds.PGPoolingDataSource">
        <property name="dataSourceName" value="Data Source" />
        <property name="serverName" value="localhost" />
        <property name="databaseName" value="java_web_app_db" />
        <property name="user" value="java_web_app" />
        <property name="password" value="password" />
        <property name="maxConnections" value="10" />
    </bean>

    <!-- Transaction Manager -->
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource" />
    </bean>

    <!-- Sql Session factory -->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource" />
    </bean>

    <!-- MyBatis scanner -->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="com.ipastushenko.core.web.repository.mapper" />
    </bean>

    <!-- User Service -->
    <bean id="userRepository" class="com.ipastushenko.core.web.repository.UserRepository" />
    <bean id="userService" class="com.ipastushenko.core.web.service.UserService" />
</beans>